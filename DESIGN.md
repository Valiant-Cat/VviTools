# VviTools 设计说明

## 产品定位

VviTools 是一个面向桌面高频操作的插件化工具箱，不是内容型应用，也不是营销页面。核心体验应保持轻、快、可预期：

- 快捷键呼出
- 输入关键词
- 选择命令
- 执行或展示结果
- 必要时进入插件市场和设置页

## 设计原则

### 主窗口

主窗口承担搜索、插件市场、已安装插件、设置入口等职责。它不直接承载复杂工具的完整功能页。

对于 `系统剪贴板` 这类高频工具：

- 主窗口只展示应用信息、命令入口、安装状态。
- 完整剪贴板历史和操作在独立剪贴板窗口中完成。
- 不要把剪贴板历史列表嵌入主窗口。

### 视觉风格

整体参考 rubick 一类轻量桌面工具：

- 左侧为分类导航。
- 右侧为当前分类或设置内容。
- 页面使用清晰分区，不做营销式 hero。
- 控件优先使用熟悉形态：开关、按钮、标签、列表、网格。
- 尽量减少装饰，保留足够信息密度。

主窗口和剪贴板窗口当前采用非透明、无圆角系统窗口，避免外层透明窗口和内层圆角造成双层边框。

### 设置页

设置项遵循“左侧说明、右侧状态/控件”的布局：

- 可切换项使用滑动开关。
- 只读项使用轻量胶囊标签。
- 开关文案必须表达真实语义。

当前设置语义：

- `开机启动`：开启后登录系统自动启动 VviTools。
- `Dock 栏显示`：开启后在 Dock 显示图标；关闭后隐藏 Dock 图标，但状态栏入口仍保留。
- `全局快捷键`：只展示当前快捷键，不在设置页内编辑。
- `点击外部关闭`：只展示当前行为。

### 插件市场

插件市场按标签分类展示插件。分类是插件标签，不是 JSON 或开发调试页面。

底部导航只保留：

- 已安装
- 设置

首页不要用“已安装”“插件市场”伪装成推荐内容。

## 剪贴板窗口

剪贴板窗口是独立窗口，通过 `Alt + V` 从底部打开。它的目标是快速选择历史内容并粘贴回原输入框。

核心交互：

- 默认选中第一项。
- 方向键移动选中项。
- 回车复制并关闭窗口。
- 点击 item 复制并关闭窗口。
- 再按快捷键、按 Escape、点击外部都关闭窗口。
- 收藏、复制、删除按钮不能遮挡图片预览。

支持类型：

- 文本
- 图片
- 文件

PDF 等文件应归类为文件，不应被当成图片。

## 插件协议

插件通过 `plugin.json` 声明：

- 插件身份：`id`、`name`、`version`、`description`
- 检索信息：`keywords`
- 运行时：`runtime`
- 入口：`entry`
- 权限：`permissions`
- 命令：`commands`

第一版运行时：

- `node`
- `shell`
- `builtin`

`builtin` 只允许捆绑内置插件使用。用户自定义插件不能导入 `builtin` 运行时。

## 内置插件

内置插件放在项目根目录 `plugins/` 下，并随应用资源打包。

`系统剪贴板` 的内部 ID 是 `dev.vvicat.system-clipboard`，这是稳定技术标识，不是用户可见名称。UI 中应展示：

- 插件名：`系统剪贴板`
- 类型：`捆绑内置` / `宿主剪贴板能力`

不要在用户可见卡片、标题、推荐、设置、已安装详情里直接展示内部 ID。

## 权限模型

普通插件不能直接访问高权限系统能力。插件应通过 manifest 声明权限，由安装流程确认，再通过宿主提供的受控接口执行。

剪贴板、全局快捷键、窗口管理、前台应用恢复与自动粘贴都属于宿主能力。

## 打包资源路径

开发环境中的内置插件路径是项目根目录下的 `plugins/`。

正式 macOS 包中资源可能位于：

```text
VviTools.app/Contents/Resources/_up_/plugins
```

因此判断插件是否内置时，不能只依赖源码目录路径；`runtime: "builtin"` 也应视为内置插件。
