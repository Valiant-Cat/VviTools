# AGENTS.md

## 回复与文档

- 默认使用中文回复、注释和文档，除非用户明确要求其他语言。
- 面向用户的说明要直接说明已做什么、如何验证、还有什么风险。
- 不要把内部实现细节伪装成产品卖点。

## 项目定位

VviTools 是 Tauri v2 + Svelte + Rust 的插件化桌面工具箱。当前核心目标是极简、快速、稳定，而不是做复杂平台化 UI。

关键产品事实：

- 主窗口是搜索、插件市场、已安装插件和设置入口。
- 剪贴板完整功能在独立剪贴板窗口，不嵌入主窗口。
- `系统剪贴板` 是捆绑内置插件，目录在 `plugins/system-clipboard`。
- 剪贴板高权限能力由 Rust 宿主实现，插件目录负责 manifest、图标、内置桥接契约和应用信息。
- 用户界面展示 `系统剪贴板`，不要展示内部 ID `system-clipboard`。

## 开发约束

- 改代码前先阅读相关现有实现，优先沿用当前结构。
- 手动编辑文件使用 `apply_patch`。
- 不要回滚用户或其他 Agent 的未提交改动。
- 不要引入无关重构。
- 不要把 demo / 测试插件暴露给正式用户。
- `文本工具`、`echo` 这类早期测试插件不应出现在正式插件市场或搜索结果中。

## UI 约束

- 工具型页面保持克制、清晰、信息密度适中。
- 不做 landing page、hero、营销式布局。
- 可切换设置使用滑动开关；只读状态使用轻量标签。
- 主窗口和剪贴板窗口当前保持非透明、无圆角，避免双层边框。
- 文案必须表达真实行为：
  - `Dock 栏显示` 开启表示显示 Dock 图标。
  - 状态栏入口应独立保留，不要和 Dock 显示混淆。
- 内部 ID、运行时入口、manifest 字段不应直接出现在普通用户可见位置，除非页面明确是开发者调试页。

## 插件约束

- 插件协议以 `plugin.json` 为入口。
- 第一版运行时支持 `node`、`shell`、`builtin`。
- `builtin` 只允许捆绑内置插件使用；自定义插件导入不得允许 `builtin` runtime。
- 插件市场采用静态 JSON + zip 包。
- 安装前必须展示 manifest 权限确认。
- 自定义插件删除只能影响用户插件目录，不能影响 `plugins/` 下的捆绑内置插件。

## 剪贴板约束

- 剪贴板窗口通过 `Alt + V` 打开。
- 打开后默认选中第一项。
- 方向键移动选中项。
- 回车或点击 item 后复制、关闭窗口，并尝试粘贴回原前台输入框。
- Escape、再次按快捷键、点击外部都应关闭窗口。
- 图片和文件要区分；PDF 属于文件，不属于图片。
- 收藏、复制、删除等操作按钮不能遮挡图片预览主体。

## 验证命令

常规改动至少运行：

```bash
npm run build
npm run test
cargo fmt --manifest-path src-tauri/Cargo.toml --check
```

涉及 Tauri、Rust 命令、窗口、状态栏、快捷键、剪贴板、打包资源路径时，还要运行：

```bash
npm run tauri -- build
```

## Git

- 默认推送到当前 `origin main`。
- 提交前确认 `git status --short`，只提交本轮相关文件。
- 如果本地 hook 要求通过 `memhub run` 执行 git 命令，按 hook 提示处理。
